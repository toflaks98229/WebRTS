# 용병단 — Mercenary Company

Battle Brothers 를 모델로 한 웹 게임. **캠페인 지도**에서 용병단을 이끌고 다니다
적 무리와 부딪히면 **헥스 전술 전투**로 전환됩니다.
빌드 도구 없이 순수 HTML + ES 모듈 + Canvas 2D 로 동작합니다.

## 실행

ES 모듈은 `file://` 에서 로드되지 않으므로 로컬 서버가 필요합니다.

```bash
npm start
```

그 뒤 <http://localhost:5173> 을 엽니다. (의존성 없음 — `serve.js` 는 Node 표준 라이브러리만 씁니다.)

에셋을 처음 받으려면:

```bash
node tools/fetch-lpc.mjs
```

```bash
node tools/fetch-dcss.mjs
```

앞은 캐릭터 스프라이트(LPC), 뒤는 UI 아이콘과 지형 타일(DCSS)입니다. 둘 다 없어도
게임은 그대로 실행됩니다 — 지형은 단색으로, 유닛은 자체 도형으로, 아이콘은
이모지로 되돌아갑니다.

## 조작

### 캠페인 지도

| 입력 | 동작 |
| --- | --- |
| 좌클릭 | 그 지점으로 행군 (적 무리를 클릭하면 추격) |
| 우클릭 / `Esc` | 행군 중지 |
| `Space` | 일시정지 · 재개 |
| `1` / `2` | 보통 / 빠름 |
| `C` | 단원 · 특성 창 |
| `L` | 실험실 |
| `Tab` | 용병단으로 카메라 복귀 |

### 전투

| 입력 | 동작 |
| --- | --- |
| 좌클릭 | 빈 칸이면 이동, 적이면 선택한 기술로 공격 |
| 우클릭 / `Esc` | 선택한 기술 해제 |
| `1`–`9` | 기술 선택 |
| `Space` | 턴 종료 |
| `Q` | 대기 (이번 라운드 순서를 뒤로 미룸) |
| `Tab` | 현재 유닛으로 카메라 복귀 |

공통으로 휠은 확대·축소, 우클릭 드래그와 방향키(또는 `WASD`)는 화면 이동입니다.

## 캠페인

- 섬 하나가 절차적으로 생성된다 — 지형은 고도와 습도 노이즈로 결정되고,
  그 위에 **마을·읍·도시**가 서로 며칠 거리를 두고 놓이며 **길**로 이어진다.
  거친 지형에는 산적 **야영지**가 숨는다.
- 시간은 실시간으로 흐른다. 행군 시간은 지형에 따라 3~11시간이고 **길 위에서는 절반**이다.
- 적 무리는 야영지 근처를 배회하다 **4칸** 안에 들어온 용병단을 추격한다.
  같은 칸에 서면 전투가 시작되고, 지형에 맞는 전장이 생성된다.
- 전투 결과는 지도로 돌아온다 — **부상과 깨진 방어구는 그대로 남고**, 전사자는 영원히 사라지며,
  승리하면 적의 장비를 노획한다.
- 마을에 머물면 상처와 장비가 서서히 회복된다. 규모가 클수록 빠르다.
  `하루 휴식` 은 하루를 통째로 건너뛴다.
- 야영지를 비워도 며칠 뒤 새 무리가 채워진다.

## 계약과 살림

마을에서 **마을 들어가기** 를 누르면 게시판·고용·상점·창고가 열린다. 머무는
동안 시간은 멈춘다.

- **계약** — 두 종류가 걸린다.
  - *산적 소탕*: 지정된 야영지의 무리를 없앤 뒤 **의뢰한 마을로 돌아가** 보고해야 보수를 받는다.
  - *대상 호위*: 목적지 마을에 도착하면 그 자리에서 삯을 받는다.
  기한을 넘기면 계약은 파기된다. 게시판은 며칠마다 새로 걸린다.
- **고용** — 지원자는 미리 굴린 실제 능력치를 보여준다. 같은 배경이라도 사람마다
  다르므로, 값을 치를지는 숫자를 보고 정한다. 큰 도시일수록 좋은 사람이 온다.
  정원은 9명.
- **상점** — 무기·방패·갑옷·투구를 사고판다. 큰 도시일수록 비싼 물건을 취급한다.
- **창고** — 전투에서 노획한 여분 장비가 쌓인다. 팔거나 다른 단원에게 넘길 수 있다.
  넘길 때 밀려난 장비는 다시 창고로 돌아온다.

**급여는 매일 새벽 자동으로 나간다.** 못 주는 날이 이틀 이어지면 단원이 떠난다
(마지막 한 명은 남는다). 초기 자금은 900 크라운이다.

## 성장

### 레벨과 특성

전투가 끝나면 경험치가 나눠진다 — 절반은 마지막 일격을 넣은 사람에게, 절반은
살아남은 전원에게 고르게. 레벨이 오르면 능력치가 조금씩 붙고 **특성 점수 1점**을
받는다. 최고 11레벨.

특성은 **6개 티어 × 4개**, 총 24종이다. 티어는 그 레벨에 도달해야 열린다.
행동바의 <b>단원</b> 버튼이나 왼쪽 명부를 눌러(또는 `C`) 특성 창을 연다.
**단원마다 따로 배치**하므로, 같은 배경이라도 방패잡이와 돌격병을 다르게 키울 수 있다.

효과는 범용 엔진이 아니라 필요한 계산 지점에 직접 박혀 있다 —
`grep colossus src/` 하면 그 특성이 하는 일이 전부 나온다.

아이콘은 [Dungeon Crawl Stone Soup](https://github.com/crawl/crawl) 의 32px 타일이라
LPC 스프라이트와 같은 픽셀 아트 결을 유지한다. 화면은 전장과 같은 **육각형 노드**로 짜여 있다. 왼쪽 티어 레일이 도달한
줄까지 금색으로 채워지고, 잠긴 줄은 `N 레벨` 배지와 함께 물러난다.
노드는 **습득 · 가능 · 대기 · 잠김** 네 상태로 구분되고, 남은 점수는
머리말의 진행 링이 알린다. 마우스를 올리면 전체 설명과 지금 왜 못 찍는지가
뜬다.

### 단장 특성

단장 트리는 **용병단에 귀속된다.** 단장이 쓰러지면 가장 노련한 생존자가
자리를 잇고, 그동안 쌓은 특성은 그대로 남는다.

점수는 **명성**으로 산다 (계약 완수와 전투 승리로 획득, 120 명성당 1점).
세 갈래가 있다:

| 갈래 | 성격 |
| --- | --- |
| 지휘 | 결의·피로·명중·사기 판정 — 부대 전체에 적용 |
| 경영 | 급여 절감, 매매가, 계약 보수, 회복 속도 |
| 무예 | 단장 본인의 명중·방어·행동력, 인접 아군 명중 |

각 갈래는 **뿌리 → 갈래 둘 → 마무리** 다이아몬드다. 선행 관계는 노드 상자를
실제로 측정해 그린 **SVG 베지어 곡선**으로 잇고, 선행이 채워지면 초록으로
살아난다. 노드에 마우스를 올리면 그 노드까지 이어지는 **선행 사슬 전체**가
금색으로 밝아져서, 마무리 특성 하나를 찍으려면 무엇이 필요한지 한눈에 보인다.
(연결선 방식은 [Hkattelu/SkillTree](https://github.com/Hkattelu/SkillTree) 와
[beautiful-skill-tree](https://github.com/andrico1234/beautiful-skill-tree) 를 참고했다.)

## 실험실 (디버그)

상단 <b>실험실</b> 버튼 또는 `L` 키. 캠페인 상태는 전혀 건드리지 않는다.

- 왼쪽 팔레트에서 **템플릿 · 진영 · 레벨 · 특성**을 정하고 **좌클릭**으로 배치, **우클릭**으로 제거.
- 지형(평지/숲/구릉/늪)을 바꾸거나 같은 배치에 새 지형을 깔 수 있다 (`R`).
- `Space` 로 배치 그대로 전투를 열고, 끝나면 같은 배치로 돌아온다.
- 특성 조합과 전투 수식을 캠페인과 무관하게 격리해서 확인하기 위한 화면이다.

## 전투 규칙

- **행동력(AP)** 매 턴 9. 이동은 지형에 따라 2~5 AP, 한 단계 오르막마다 +2 AP.
- **피로도** 행동마다 누적되어 방어력과 주도권을 깎고, 한계에 닿으면 기술을 못 쓴다. 턴마다 15 회복.
- **지배 지역(ZOC)** 적에게 인접한 칸에서 벗어나면 추가 AP·피로도를 지불한다.
- **방어구** 피해는 방어구 내구도를 먼저 깎고 일부만 체력에 들어간다.
  둔기는 관통(`armorPen`)이, 도끼는 방어구 파괴(`armorMult`)가 강하다.
- **부위** 25% 확률로 머리에 맞고 피해가 1.5배가 된다.
- **고저차** 높은 곳에서 치면 명중 +10/단계, 피해 +15%/단계. 능선은 시야를 막는다.
- **사기** 확신 → 침착 → 동요 → 붕괴 → 패주. 아군의 죽음과 중상이 판정을 부른다.
- **영구 사망** 쓰러진 단원은 돌아오지 않는다. 승리하면 적의 장비를 노획해 생존자가 장비한다.

## 구조

```
index.html            화면 구성
css/style.css         UI 스타일
serve.js              의존성 없는 정적 서버
tools/fetch-lpc.mjs   LPC 스프라이트 선별 다운로더
tools/fetch-dcss.mjs  DCSS UI 아이콘 다운로더 (라이선스 제외 목록 검사 포함)

tools/lpc-attribution.mjs  에셋 출처 표기 자동 생성

src/
  main.js             앱 셸 — 캔버스, 지속되는 용병단, 씬 전환, 입력 라우팅
  scenes/
    campaignScene.js  지도 씬 — 시간 진행, 행군 명령, 조우 감지
    battleScene.js    전투 씬 — 입력, 연출, AI 진행, 전과 보고
    labScene.js       실험실 — 유닛 배치 샌드박스
  core/               시드 RNG, 이벤트 버스
  hex/                헥스 좌표계(hex.js), 픽셀 변환(layout.js), 경로탐색(pathfind.js)
  data/               지형 · 월드 지형 · 장비 · 기술 · 유닛 템플릿
                      perks(특성 24종·경험치 표) · captainTree(단장 3갈래)
  battle/             grid(높이맵) · unit · combat(명중·피해) · battle(턴 진행) · ai
  campaign/           world(맵 생성) · campaign(시간·부대·조우·계약·급여)
                      company(명부·금고·창고) · contracts · bands · loot
  render/             camera · renderer(전장) · worldRenderer(지도) · sprites(LPC)
                      terrainAtlas(DCSS 지형 패턴) · effects
  ui/                 overlay(공용) · hud(전투) · worldHud(지도)
                      settlementPanel(마을) · characterPanel(특성·단장 트리)
                      perkView(특성 노드 공용 렌더) · icons(DCSS 타일 로더)
```

모델 계층(`src/battle`, `src/hex`, `src/data`, `src/campaign`)은 DOM 을 전혀 참조하지
않아 Node 에서 그대로 시뮬레이션을 돌릴 수 있습니다. 밸런싱과 지도 생성 검증은
그 방식으로 했습니다.

## 라이선스

| 대상 | 라이선스 |
| --- | --- |
| 코드 | GPL-3.0-or-later — [LICENSE](LICENSE) |
| 캐릭터 아트 (`assets/lpc/`) | 파일마다 상이 — [ATTRIBUTION.md](assets/lpc/ATTRIBUTION.md) |
| UI 아이콘 · 지형 타일 (`assets/dcss/`) | CC0 1.0 — Dungeon Crawl Stone Soup |

번들된 LPC 아트는 CC0 · CC-BY · CC-BY-SA · OGA-BY · GPL 이 섞여 있고, 일부는
CC-BY-SA 3.0 단독이라 GPL 로 덮을 수 없습니다. 재배포 시 조건은
[CREDITS.md](CREDITS.md) 를 확인하세요.
