# 크레딧 및 라이선스

## 요약

| 대상 | 라이선스 |
| --- | --- |
| 코드 | GPL-3.0-or-later — [LICENSE](LICENSE) |
| 아트 (`assets/dcss/`) | **CC0 1.0** (퍼블릭 도메인) — Dungeon Crawl Stone Soup |
| 폰트 | SIL Open Font License 1.1 (Google Fonts CDN) |

아트가 전부 CC0라 **아트 때문에 프로젝트 라이선스가 제약받지 않습니다.**
표기 의무도 없지만 예의로 남깁니다.

> 이전에는 캐릭터에 LPC 스프라이트(CC-BY-SA 3.0 / GPL-3.0)를 썼고, 그중 일부는
> CC-BY-SA 단독이라 GPL 로 덮을 수 없어 코드와 아트의 라이선스를 갈라 두어야
> 했습니다. 아트를 DCSS로 통일하면서 그 제약이 사라졌습니다.

## 아트 — Dungeon Crawl Stone Soup

`assets/dcss/` 의 모든 이미지는 [DCSS](https://github.com/crawl/crawl) 의
rltiles 에서 가져왔습니다. `tools/fetch-dcss.mjs` 가 필요한 것만 내려받습니다.

```bash
node tools/fetch-dcss.mjs
```

- UI 아이콘 77개 (특성 · 단장 특성 · 전투 기술 · 장비)
- 지형 타일 64개 (18개 지형 세트 + 나무)
- 유닛 스프라이트 27장 (13개 배경 · 인간형 몬스터 타일)
- 손에 든 장비 18장 (무기 15 · 방패 3)

**원작자**: DCSS 타일 아티스트 다수. 재사용을 허락한 작가 목록은
[crawl/tiles ARTISTS.md](https://github.com/crawl/tiles/blob/master/ARTISTS.md)
에 있습니다.

### 라이선스가 불분명한 타일은 자동으로 걸러냅니다

DCSS 공식 `LICENSE` 는 "타일 대부분은 CC0 이지만 오래된 것은 상황이 복잡할 수
있다"고 밝히고, 소유권을 확인하지 못한 타일 목록을 따로 관리합니다 —
[TILES_UNDER_UNKNOWN_LICENSE.md](https://github.com/crawl/tiles/blob/master/TILES_UNDER_UNKNOWN_LICENSE.md).

`tools/fetch-dcss.mjs` 는 **받을 때마다 그 목록을 내려받아 대조하고, 하나라도
걸리면 매니페스트를 쓰지 않고 비정상 종료합니다.** 작성 시점에 한 번 확인하고
마는 대신 매번 검사하므로, 목록이 갱신되어도 자동으로 반영됩니다.

실제로 이 검사에 걸려 대체한 것들:

| 쓰려던 타일 | 대체 |
| --- | --- |
| `javelin1.png` (투창) — 모든 javelin 타일이 제외 | `trident1.png` |
| `stealth.png` (협공) | `camouflage_1.png` |
| `tree1.png`, `tree2.png` | `tree3/5/7/9.png` |

에셋이 없어도 게임은 그대로 동작합니다 — 지형은 단색, 유닛은 자체 도형,
아이콘은 이모지로 폴백합니다.

### 던전 타일을 야외에 쓰지 않기

DCSS 는 던전 게임이라 `dngn/wall/` 타일 대부분이 **모르타르 줄눈이 보이는
석조**입니다. 그대로 쓰면 절벽 면과 산봉우리가 성벽처럼 보입니다. 그래서 바위 ·
절벽 · 고봉은 벽 타일 대신 자연석 바닥 타일을 씁니다 — 바위와 고봉은
`limestone`(밝은 맨바위), 절벽 면은 `grey_dirt`(어두운 자갈), 산악은
`pebble_brown`(갈색 너덜) 입니다.

## 정사각 타일을 육각형에 입히기

DCSS 타일은 **32×32 정사각형**이라 육각형에 그대로 얹을 수 없습니다. 세 단계를
거칩니다:

1. **변형 타일을 이어 붙여 큰 시트를 만든다.** 32px 하나를 그대로 반복하면
   벽지처럼 보이므로, 같은 지형의 변형 4종을 4×4로 엮어 128px 시트를 만들고
   그것을 반복 패턴으로 씁니다.
2. **헥스 경로로 클리핑한다.** 패턴을 육각형 path 에 채웁니다. 모양은 육각형이
   정하고 무늬는 **월드 좌표에 고정**되므로, 같은 지형끼리 붙어 있으면 경계
   없이 하나의 들판으로 이어집니다. 칸마다 정사각형이 찍히지 않습니다.
3. **경계를 서로 번지게 한다.** DCSS 에는 방향별 전이 타일(`grass_n`, `grass_ne`
   …)이 있지만 정사각 그리드의 8방향용이라 육각형의 6방향에 맞지 않습니다.
   그래서 전이 타일을 빌려오는 대신, 칸마다 자기 캔버스에 **바닥을 깔고
   우선순위가 높은 이웃을 맞닿은 변에서부터 페더링해 겹칩니다.** 어느 변이 어느
   이웃을 향하는지는 각도를 가정하지 않고 레이아웃 기하에서 직접 구합니다.

번지는 방향은 지형 데이터의 `blend` 값이 정합니다 — 물이 뭍으로, 진흙이 풀밭으로
번지고, 맨바위에는 아무것도 번지지 않습니다.

합성 결과는 칸에 캐시되므로 비용은 맵 생성 시점에 한 번만 들고, 그리기는 칸당
`drawImage` 한 번입니다. 화면 밖 칸은 합성도 그리기도 건너뜁니다.
구현은 [src/render/terrainAtlas.js](src/render/terrainAtlas.js).

## 하나의 픽셀 배율

**타일을 확대해서 채우면 안 됩니다.** 바닥만 2배로 늘리고 나무와 유닛은 원본
크기로 두면 픽셀 격자가 서로 어긋나 화면이 지저분해집니다.

그래서 지형 · 나무 · 유닛이 **전부 32px DCSS 아트**이고, 모두 같은 배율
(`PIXEL`, 전장 2배 / 지도 1.4배)로 그립니다. 소스 픽셀 하나가 화면에서 언제나
같은 크기입니다. 나무는 확대하지 않고 원본 크기 그대로 칸당 한 그루만 세웁니다 —
DCSS 나무 타일은 나무 한 그루가 아니라 숲 한 칸을 뜻하기 때문입니다.

## 유닛은 인간형 몬스터 타일 + 무기·방패 두 겹

전투원의 **몸은 DCSS 몬스터 타일 한 장**이고, 그 위에 한눈에 읽혀야 하는 두 가지
— **무기는 뒤, 방패는 앞** — 만 덧그립니다. 갑옷은 겹치지 않고 유닛 카드에서
읽습니다.

처음에는 여섯 겹짜리 페이퍼돌이었고, 방패가 매번 어긋났습니다. DCSS의 방패
그림은 그쪽 합성기가 *몸 뒤 조각*과 *팔 앞 조각*으로 나눠 그리도록 잘려 있는데
우리는 뒤 조각만 갖고 있었습니다. z 순서를 어떻게 바꿔도 방패가 손 뒤로
들어갑니다 — 앞 절반의 그림 자체가 없으니까요. 그래서 몸은 한 명이 통째로 그린
그림으로 바꾸고, 겹치는 것은 두 장으로 줄였습니다. **방패를 몸통 전체 앞**에
두면 손에 삼켜질 일이 없고, **무기를 뒤**에 두면 실루엣이 몸통과 겹치지 않습니다.

### 손 위치는 DCSS 의 표를 그대로 씁니다

`player/hand1`·`hand2` 는 **페이퍼돌 기본 몸(`player/base/human_m`)의 어깨에
맞춰 잘린** 그림입니다. 몬스터마다 팔이 다른 자리에 있으니 그대로 얹으면 무기가
손에서 한 뼘 떨어져 뜹니다.

DCSS 도 같은 문제를 겪고, 같은 방식으로 풉니다. 몬스터 타일 위에 장비를 얹는
곳은 [`tiledoll.cc`](https://github.com/crawl/crawl/blob/master/crawl-ref/source/tiledoll.cc)
의 `pack_doll_buf` 한 군데뿐이고, 거기서 손 위치를
[`tilemcache.cc`](https://github.com/crawl/crawl/blob/master/crawl-ref/source/tilemcache.cc)
의 `mcache_monster::get_weapon_offset` / `get_shield_offset` 에게 물어봅니다.
**타일별로 손으로 맞춘 픽셀 오프셋 표**이고, 작성 시점 기준 무기 341개 · 방패
161개 항목입니다. 우리는 이 숫자를 그대로 가져다 씁니다.

그 표의 주석 두 줄이 타일 선택까지 제약합니다:

> *"Tiles showing monsters already wielding a weapon should not be listed here."*
> — 이미 무기를 든 그림에는 얹지 않는다. 목록에 없으면 `default: return false`,
> 즉 **"이 몬스터에는 무기를 그릴 수 없다"** 입니다.

> *"No monster should have a shield hard-drawn on the tile."*
> — 방패도 같은 규칙.

그래서 로스터는 **DCSS 무기 오프셋 표에 있는 타일로만** 골랐습니다. 이 게임의
전투원은 예외 없이 무기를 드니까요. 이 과정에서 두 장이 탈락했습니다:

- `edmund_weaponless` — DCSS 가 *에드먼드가 무기를 잃었을 때* 쓰는 컷이라
  ([`tilepick.cc`](https://github.com/crawl/crawl/blob/master/crawl-ref/source/tilepick.cc)
  `MONS_EDMUND`), 무기 오프셋이 아예 없습니다.
- `slave_freed` — 그림 파일은 저장소에 남아 있지만 `dc-mon.txt` · `dc-player.txt`
  어디에서도 참조하지 않는 **고아 에셋**입니다. 데이터가 있을 리 없습니다.

방패 오프셋이 없는 네 장(`imperial_myrmidon` · `terence` · `joseph` · `rupert`)은
DCSS 가 그 몬스터에게 방패를 준 적이 없어 아무도 숫자를 맞춰 본 적이 없는
경우입니다. 우리 단원은 방패를 자유롭게 노획하므로, 이 넷은 DCSS 의 "no shift
necessary" 값인 페이퍼돌 기본 위치(0,0)로 떨어집니다.

배경마다 스프라이트를 두세 장씩 두고 유닛 id 로 하나를 고르므로, 민병대 여섯
명이 같은 사람 여섯이 되지 않고 한 단원은 죽을 때까지 같은 얼굴을 유지합니다.
아군과 산적의 목록은 일부러 겹칩니다 — 가죽 갑옷 입은 약탈자와 가죽 갑옷 입은
용병은 같은 그림이고, 어느 편인지는 발밑의 진영 고리가 말합니다.
구현은 [src/render/unitArt.js](src/render/unitArt.js).

## 정지 그림으로 방향과 움직임 만들기

몬스터 타일은 **정면 정지 프레임 한 장**입니다. 걷기·공격 프레임도, 4방향
스프라이트도 없으므로 움직임과 방향은 렌더러가 만듭니다:

- **이동**: 걸어가는 쪽을 봅니다. 다만 육각형 여섯 방향 중 둘은 정위와 정하라
  좌우 정보가 없습니다. 그 걸음에서는 방향을 뒤집는 대신 **행군 전체의 방향**을
  기억해 뒀다가 씁니다.
- **공격**: 상대 쪽을 보고, 맞은 쪽은 **가해자 쪽으로 돌아섭니다.** 바로 위나
  아래를 치는 경우에도 좌우 정보가 없으므로 보던 방향을 유지합니다.
- 방향은 좌우 반전이라 무기와 방패도 함께 반대편으로 넘어갑니다.
- 행군 중에는 발을 구르는 상하 진동, 공격할 때는 대상 쪽으로 파고드는 런지.

## 폰트

- [Cinzel](https://fonts.google.com/specimen/Cinzel), [Noto Serif KR](https://fonts.google.com/noto)
  — SIL Open Font License 1.1, Google Fonts CDN 경유.
